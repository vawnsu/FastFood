/**
 * Chuỗi bộ lọc chạy theo thứ tự tên: bảng mã, đăng nhập, rồi phân quyền theo vai trò.
 * Toàn bộ trang JSP nằm trong WEB-INF nên không thể mở trực tiếp — mọi đường vào
 * đều phải đi qua servlet và do đó qua chuỗi bộ lọc này.
 */
package com.fastfood.filter;
